---
import { Icon } from "astro-icon/components";
import type { SocialLink } from "../types/config";

type Props = SocialLink;

const { title, url, icon, external } = Astro.props;
---

<a
  href={url}
  class="flex items-center gap-3 opacity-60 group-hover:opacity-30 hover:!opacity-100 hover:!text-accent hover:translate-x-1 transition-all duration-300"
  target={external ? "_blank" : "_self"}
>
  <Icon name={icon} width={25} height={25} />
  <div>
    <p>{title}</p>
  </div>
</a>
